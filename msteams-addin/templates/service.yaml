apiVersion: v1
kind: Service
metadata:
  name: {{ include "sendent-msteams.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "sendent-msteams.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "sendent-msteams.name" . }}