apiVersion: v1
kind: Service
metadata:
  name: {{ include "sendent-msteams.fullname" . }}
  labels:
    {{- include "sendent-msteams.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sendent-msteams.selectorLabels" . | nindent 4 }}