apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "outlook-addin.fullname" . }}
  labels:
    {{- include "outlook-addin.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
